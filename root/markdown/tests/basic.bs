use test;
use markdown;

suite BasicMarkdown {
	var simple = str {
		this is a
		paragraph

		this is the
		next *paragraph*
		with four
		lines

		single line

		this is a heading
		=================

		### deep heading

		this is another paragraph
	};

	Document result = parse(simple);

	test result.elements.count == 6;
	// print("Result:");
	// print(result.toS);
	// print("\nHTML:");
	// print(result.toHtml.toS);
}
