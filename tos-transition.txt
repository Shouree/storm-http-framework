TODO:

- Rename things in C++: find . -name "*.h" | xargs grep -rI "operator *<<.*StrBuf"
- Update logic to find toS() in Type for the Handles
- Update logic in Basic Storm that uses toS.
- Update enum (and other types in lib/ to follow the new convention)
