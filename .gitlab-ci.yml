stages:
  - test

test_job:
  stage: test
  image: ubuntu:latest
  script:
    - apt-get update
    - apt-get install -y storm-lang
    - storm -i root/http -T http
  only:
    - master
    - merge_requests
